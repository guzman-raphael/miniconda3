version: '2.4'
services:
  app:
    build:
      context: ../..
      dockerfile: dist/alpine/Dockerfile
      args:
        - CONDA_VERSION
        - CONDA_MD5
        - PY_LABEL
    image: datajoint/miniconda3:${CONDA_VERSION}-py${PY_VERSION}-alpine3.11
    command: tail -f /dev/null
