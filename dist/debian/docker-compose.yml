version: '2.4'
services:
  app:
    build:
      context: ../..
      dockerfile: dist/debian/Dockerfile
      args:
        - CONDA_VERSION
        - PY_LABEL
    image: datajoint/miniconda3:${CONDA_VERSION}-py${PY_VERSION}-debian
    command: tail -f /dev/null
